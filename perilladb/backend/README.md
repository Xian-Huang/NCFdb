# PerillaDB backend

This backend serves only the PerillaDB API and Django admin.

Default development port: `8102`

```bash
copy .env.example .env
python manage.py migrate
python manage.py runserver 8102
```
